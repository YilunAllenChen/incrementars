[package]
name = "incrementars"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
criterion = { version = "0.4", features = ["html_reports"] }
iai = "0.1"

[[bench]]
name = "time"
harness = false
